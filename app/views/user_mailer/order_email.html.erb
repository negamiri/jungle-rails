<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order!</h1>
    <p>Hi user!</p>
    <p>Your order has been placed! Nice one! Here's everything in your order:</p>
    <% @order.line_items.each do |item| %>
      <ul>
        <li><%= item.product.name %></li>
      </ul>
    <% end %>
    <h4>Total: <%= humanized_money_with_symbol @order.total_cents/100 %></h4>
  </body>
</html>